# 定时测速部署

当前 Compose 配置使用 `restart: "no"`，因此 CFST 每次完成测速并写入结果文件后会正常退出。这是预期行为。

不要改为 `restart: always` 或 `restart: unless-stopped` 来实现定时测速。这样会在每次完成后立即再次扫描大量 Cloudflare 地址，频率不可控，也可能导致多个结果被快速覆盖。

推荐使用 1Panel 的“计划任务”按固定间隔触发单次任务。本项目按每 6 小时运行一次配置；若仅需日常观察，可改为每天一次。

## 安装脚本

将 `run-cfst.sh` 上传到 VPS，然后执行：

```bash
sudo chmod 0755 /opt/dockerdata/cfst/run-cfst.sh
sudo chown -R 65532:65532 /opt/dockerdata/cfst/results
```

确认 `/opt/dockerdata/cfst/docker-compose.yml` 与 `run-cfst.sh` 位于同一目录；脚本会使用这个 Compose 文件启动测速。

手动验证一次：

```bash
/opt/dockerdata/cfst/run-cfst.sh
```

脚本会以 `docker compose run --rm cfst` 启动一次临时容器，结束后自动移除临时容器，结果仍写入 `/opt/dockerdata/cfst/results/result.csv`。它会使用 `/tmp/cfst.lock` 防止任务重叠。

## 1Panel 计划任务

1. 打开 1Panel 的“计划任务”，创建“Shell 脚本”任务。
2. 周期选择“自定义”，填写 `0 */6 * * *`，即每天 00:00、06:00、12:00、18:00 各运行一次。
3. 脚本内容填写：

```bash
/opt/dockerdata/cfst/run-cfst.sh >> /opt/dockerdata/cfst/cfst-schedule.log 2>&1
```

4. 保存后先使用“执行一次”验证，再查看 `result.csv` 和 `cfst-schedule.log`。

每天一次可使用 Cron 表达式：

```text
15 3 * * *
```

不要让计划任务间隔短于一次测速所需的时间。若手动从 1Panel/Dockge 启动测速，同时计划任务也启动，脚本会跳过计划任务的重复执行。
