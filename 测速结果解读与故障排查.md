# 测速结果解读与故障排查

## CSV 字段

```text
IP 地址,已发送,已接收,丢包率,平均延迟,下载速度(MB/s),地区码
```

- `已发送/已接收`：TCP 延迟测试包数量。`4,4` 代表四个测试包都收到回复。
- `丢包率`：延迟测试的丢包比例。`0.00` 代表该次测试未丢包。
- `平均延迟`：测试机到此 IP 的 TCP 平均延迟，单位为毫秒，越低越好。
- `下载速度(MB/s)`：通过测速地址取得的下载速度，越高越好。
- `地区码`：Cloudflare 响应头报告的边缘节点代码，例如 `HKG`、`NRT`、`LAX`。`N/A` 代表本次下载请求未成功取得该信息。

结果应优先选择同时满足以下条件的 IP：丢包率为零或很低、延迟低、下载速度高、重复多次测试仍稳定。程序会在下载测速成功后按下载速度排序，因此第一行才有参考价值。

## 下载速度为 0.00

当所有行的下载速度均为 `0.00`、地区码为 `N/A` 时，延迟测试仍然有效，但下载测速失败，不能据此判定最终优选 IP。常见原因包括默认测速地址不可用、被网络拦截、HTTPS/TLS 请求失败或返回 403/超时。

临时在 Dockge 的 `command:` 列表最后添加：

```yaml
      - -debug
```

重新启动后查看日志。`-debug` 会显示下载测速失败的具体原因。排查完成后可删除此参数。

官方默认测速地址不保证一直可用。若确认默认地址不可用，可在自己 Cloudflare 已代理的测试域名上准备一个可公开读取的大文件，并增加：

```yaml
      - -url
      - https://test.example.com/cfst.bin
```

测试域名必须经 Cloudflare 代理，文件应允许匿名 `HTTPS` 访问且返回 HTTP 200。不要将登录页、管理后台或付费下载地址用于测速。

## 测试地点决定结果适用范围

测速结果仅代表运行 CFST 的 VPS 到 Cloudflare IP 的网络路径。名称为“凤凰城”的 VPS 测出的低延迟 IP，只适合凤凰城 VPS 的出口路径，不能证明中国大陆访问者使用它也更快。

若目标是提升中国大陆用户访问网站的体验，应在目标运营商和地区的中国大陆网络中测试，至少分别验证电信、联通和移动的结果；同一个 IP 在不同网络下的延迟、丢包和下载速度可能完全不同。
