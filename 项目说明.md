# CloudflareSpeedTest Docker 化说明

该 Docker 项目只完成本地/ VPS 出口网络测速：容器通过 host 网络访问候选 Cloudflare IP，结果保存为宿主机 CSV。

它不包含 DNS 更新、第三方优选 IP 服务、代理功能或任何 API 密钥，因此可作为后续人工评估的低风险第一步。

源码固定为 CloudflareSpeedTest `v2.3.5`，在构建镜像时从官方 Git 仓库编译。上游要求 Go 1.18 或更高，本项目使用 Go 1.23 构建；运行镜像不含编译工具或 Shell。

默认参数有意降低为 `-n 50 -dn 5 -dt 5`。应先多次测试并观察 CPU、网络与结果稳定性，再调整参数。不能只根据一次 CSV 的首个候选 IP 判定长期最优。
